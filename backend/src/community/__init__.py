"""Third-party / community-contributed adapters.

This namespace hosts optional external integrations (DuckDuckGo, Jina, Tavily,
aio_sandbox, scrapling, etc.). Per topology policy (see `.importlinter`),
this package must never depend on product domains.
"""
